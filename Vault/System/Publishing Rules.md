# Publishing Rules

Only notes in `Vault/Lore/` can publish. A public note must have `publish: true` and `status: canon`, plus `title`, `description`, `slug`, and `type`.

Draft, private, template, and system notes are ignored.
